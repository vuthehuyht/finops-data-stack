version: 2
sources:
  - name: RAW
    description: Raw data source for RAW
    schema: "{{ env_var('REDSHIFT_SCHEMA_RAW', 'RAW') }}"
    tables:
      - name: RAW_FINANCIAL_RATIOS
        description: Raw data source for RAW_FINANCIAL_RATIOS
        meta:
          dagster:
            asset_key:
              - RAW
              - RAW_FINANCIAL_RATIOS
        columns:
          - name: TICKER
            data_type: VARCHAR(256)
          - name: DATE
            data_type: VARCHAR(256)
          - name: SHARES_OUTSTANDING
            data_type: VARCHAR(256)
          - name: MARKET_CAP
            data_type: VARCHAR(256)
          - name: BATCH_DATE
            data_type: DATE
          - name: _CONATA_SOURCE
            data_type: VARCHAR(256)
          - name: _CONATA_SOURCE_ROW_NUMBER
            data_type: INTEGER
          - name: _CONATA_PARTITION_KEY
            data_type: VARCHAR(256)
          - name: _CONATA_LOADED_AT
            data_type: TIMESTAMP
