version: 2
sources:
  - name: RAW
    description: Raw data source for RAW
    schema: "{{ env_var('REDSHIFT_SCHEMA_RAW', 'raw') }}"
    tables:
      - name: RAW_INTEREST_RATES
        description: Raw data source for RAW_INTEREST_RATES
        meta:
          dagster:
            asset_key:
              - RAW
              - RAW_INTEREST_RATES
        columns:
          - name: RATE_TYPE
            data_type: VARCHAR(256)
          - name: DATE
            data_type: VARCHAR(256)
          - name: RATE_VALUE
            data_type: VARCHAR(256)
          - name: BATCH_DATE
            data_type: DATE
          - name: _CONATA_SOURCE
            data_type: VARCHAR(256)
          - name: _CONATA_SOURCE_ROW_NUMBER
            data_type: INTEGER
          - name: _CONATA_PARTITION_KEY
            data_type: VARCHAR(256)
          - name: _CONATA_LOADED_AT
            data_type: TIMESTAMP
