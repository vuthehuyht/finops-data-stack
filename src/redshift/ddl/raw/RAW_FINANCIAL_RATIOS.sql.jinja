-- Recreate RAW table: RAW_FINANCIAL_RATIOS
CREATE TABLE IF NOT EXISTS "{{ schema_name_raw }}"."RAW_FINANCIAL_RATIOS" (
  TICKER VARCHAR(256),
  DATE VARCHAR(256),
  SHARES_OUTSTANDING VARCHAR(256),
  MARKET_CAP VARCHAR(256),
  BATCH_DATE DATE,
  _CONATA_SOURCE VARCHAR(256),
  _CONATA_SOURCE_ROW_NUMBER INTEGER,
  _CONATA_PARTITION_KEY VARCHAR(256),
  _CONATA_LOADED_AT TIMESTAMP
);
