-- Recreate RAW table: RAW_INTEREST_RATES
CREATE TABLE IF NOT EXISTS "{{ schema_name_raw }}"."RAW_INTEREST_RATES" (
  "RATE_TYPE" VARCHAR(256),
  "DATE" VARCHAR(256),
  "RATE_VALUE" VARCHAR(256),
  "BATCH_DATE" DATE,
  "_CONATA_SOURCE" VARCHAR(256),
  "_CONATA_SOURCE_ROW_NUMBER" INTEGER,
  "_CONATA_PARTITION_KEY" VARCHAR(256),
  "_CONATA_LOADED_AT" TIMESTAMP
);
